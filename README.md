# dokushujs

## expressプロジェクトの作成
- mkdir sample_express
- cd sample_express
- npm init -y
- npm install express
- (※)　package.jsonに"type": "module"を追加してES Moduleを有効化する。
    - EJSのインストール
    - npm install ejs